const ADD_CHALLENGERS = {
    type: '@challengers/add'
}

const ADD_GRANDMASTERS = {
    type: '@grandMasters/add'
}

const ADD_MASTERS = {
    type: '@masters/add'
}

const SET_REGION = {
    type: '@region/set'
}

const SET_ELO = {
    type: '@elo/set'
}

const RESET_RANKED = {
    type: '@ranked/reset'
}

const ACTIONS = {
    ADD_CHALLENGERS,
    ADD_GRANDMASTERS,
    ADD_MASTERS,
    SET_REGION,
    SET_ELO,
    RESET_RANKED
}

export default ACTIONS